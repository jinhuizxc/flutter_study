
import 'package:flutter/cupertino.dart';

/*
 * 用 SQLite 做数据持久化
 * https://flutter.cn/docs/cookbook/persistence/sqlite
 */
class SqliteTest extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
